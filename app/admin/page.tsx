import Link from "next/link";
import { prisma } from "@/lib/prisma";
export default async function Admin(){const[dramas,episodes,published,drafts]=await Promise.all([prisma.drama.count(),prisma.episode.count(),prisma.episode.count({where:{status:"PUBLISHED"}}),prisma.episode.count({where:{status:"DRAFT"}})]);return <section className="admin-content"><div className="row" style={{justifyContent:"space-between"}}><div><h1>Dashboard</h1><p style={{color:"#777"}}>Manage dramas, episodes and media.</p></div><Link className="btn btn-red" href="/admin/dramas/new">+ New drama</Link></div><div className="grid" style={{gridTemplateColumns:"repeat(auto-fit,minmax(180px,1fr))",gap:14,marginTop:24}}>{[["Dramas",dramas],["Episodes",episodes],["Published",published],["Drafts",drafts]].map(x=><div className="stat" key={x[0]}><div style={{color:"#777",fontSize:13}}>{x[0]}</div><div style={{fontSize:32,fontWeight:950,marginTop:6}}>{x[1]}</div></div>)}</div></section>}
