"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export default function Login(){const router=useRouter();const[email,setEmail]=useState("");const[pw,setPw]=useState("");const[err,setErr]=useState("");const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setErr("");const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password:pw})});const d=await r.json();if(!r.ok){setErr(d.error||"Login failed");setBusy(false);return}router.replace("/admin")}return <main style={{minHeight:"100vh",display:"grid",placeItems:"center",background:"#080808",padding:20}}><form onSubmit={submit} className="card" style={{width:"min(420px,100%)",padding:28}}><h1 style={{marginTop:0}}>Admin Login</h1><p className="muted">Manage your free streaming website.</p><label>Email<input className="input" style={{marginTop:6}} value={email} onChange={e=>setEmail(e.target.value)} type="email" required autoComplete="username"/></label><label style={{display:"block",marginTop:14}}>Password<input className="input" style={{marginTop:6}} value={pw} onChange={e=>setPw(e.target.value)} type="password" required autoComplete="current-password"/></label>{err&&<p style={{color:"#f87171"}}>{err}</p>}<button className="btn btn-red" style={{width:"100%",marginTop:16}} disabled={busy}>{busy?"Signing in…":"Sign in"}</button></form></main>}
