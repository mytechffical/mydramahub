"use client";
import { useEffect,useRef } from "react";
export default function ClassicEditor({value,onChange}:{value:string;onChange:(v:string)=>void}){const ref=useRef<HTMLDivElement>(null);useEffect(()=>{if(ref.current&&ref.current.innerHTML!==value)ref.current.innerHTML=value},[value]);function cmd(c:string){document.execCommand(c,false);ref.current?.focus();onChange(ref.current?.innerHTML||"")}return <div><div className="row gap-2" style={{padding:8,background:"#eee",border:"1px solid #ccc",borderBottom:0,borderRadius:"9px 9px 0 0"}}>{["bold","italic","underline"].map(c=><button type="button" key={c} onClick={()=>cmd(c)} className="btn btn-dark" style={{padding:"6px 10px",fontSize:12}}>{c}</button>)}<button type="button" onClick={()=>cmd("insertUnorderedList")} className="btn btn-dark" style={{padding:"6px 10px",fontSize:12}}>• List</button></div><div ref={ref} contentEditable suppressContentEditableWarning className="editor" onInput={()=>onChange(ref.current?.innerHTML||"")} /></div>}
